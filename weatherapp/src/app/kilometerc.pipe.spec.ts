import { KilometercPipe } from './kilometerc.pipe';

describe('KilometercPipe', () => {
  it('create an instance', () => {
    const pipe = new KilometercPipe();
    expect(pipe).toBeTruthy();
  });
});
